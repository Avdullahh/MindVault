- The following is a list of issues were encountered when using the application:

Optimize for production build: TBD

---

ISSUE 1: Goals should link to both ideas and projects
Status: Done
Clarified:
- Goal → Idea linking already exists and works (goal_ideas junction + picker UI on goal detail screen). No changes needed to that side.
- Goal → Project: currently a single project_id FK (used by the AI plan flow — keep it). Add a new goal_projects junction table for many-to-many manual links.
- Add a "Link project" picker section to the goal detail screen, same toggle pattern as the existing "Link idea" section.
- The junction table is for manual links only. The AI plan flow continues to write project_id directly.

ISSUE 2: Project tasks selectable from a linked goal
Status: Done
Clarified:
- On the goal detail screen, add a task picker that shows ONLY tasks where task.project_id matches one of the goal's linked projects (via goal_projects junction or project_id FK).
- Selecting a task creates a task_goals row (task stays in its project, also appears on the goal).
- On the goal detail screen, linked tasks are displayed as READ-ONLY progress indicators — they cannot be checked off from the goal. Completion only happens from the project view.
- Checking tasks off in the project drives the goal's visible completion progress.

ISSUE 3: Projects should link to both ideas and goals
Status: Done
Clarified:
- Project → Idea linking already exists and works (project_ideas junction + picker UI). No changes needed.
- Project → Goal: add a goals picker section to the project detail screen, same toggle pattern as the ideas picker.
- This uses the same goal_projects junction table introduced in Issue 1, linked from the project side.

ISSUE 4: New app icon, in-app logo, and theme identity
Status: Done
Clarified:
- Codex to recreate the icon from scratch as SVG/code using the attached reference image as the design guide.
  The mark is: MV letterform (shield shape with M at top, V at bottom), gold metallic finish, brain-maze with keyhole at centre.
  Two versions exist: dark-brown background (matches app's leather/dark theme) and light-grey background.
- Use the dark-brown background version as the single static iOS app icon (matches existing leather/brown app theme).
- App icon must be a 1024×1024 PNG, correct for iOS App Store and Expo asset pipeline. Add to app.json.
- Test visibility in Expo Go.
- Also update in-app branding: the login screen title/logo area should display the new MV shield+brain mark.

ISSUE 5: UI centralisation across all views
Status: Done
Clarified:
- Apply both: consistent equal left/right padding globally AND horizontal centering where appropriate.
- Worst offenders: Home/Dashboard screen and all list screens (Ideas, Goals, Projects) — currently feel stretched and left-heavy.
- Fix: enforce a uniform horizontal padding (e.g. px-5 consistently), center cards/headings/buttons on the home screen, ensure lists don't appear flush-left.

ISSUE 6: Light / Dark / System theme toggle + settings page
Status: Done
Clarified:
- Remove the settings gear icon from the Ideas screen.
- Add settings gear icon to the Home/Dashboard screen top-right corner ONLY. Settings is not accessible from any other screen.
- Build a new settings page (app/(app)/settings.tsx or similar) with:
    - Theme section: radio list with three options — Light, Dark, System (copy phone appearance).
    - Dark mode = the existing leather/brown palette (already implemented).
    - Light mode = new lighter palette: white/off-white backgrounds, gold accents, dark-brown text.
    - System = follow the phone's system appearance preference automatically.
- Theme choice is persisted in AsyncStorage (not sensitive, no need for SecureStore).

ISSUE 7: Calendar event time overflows to two lines for double-digit hours
Status: Done
Clarified:
- When an event time is 4 characters wide (e.g. '10:00PM'), the time string wraps onto a second line in the calendar day/week cell.
- Fix: shorten the time format to prevent overflow. Suggested format: drop minutes when :00, use lowercase am/pm (e.g. '10am', '9:30pm'). Double-digit hours that still overflow should drop AM/PM entirely and show only the time.
- Apply the fix wherever event times are rendered in the calendar views.

ISSUE 8: Settings page includes personal information management
Status: Done
Clarified:
- The settings page (from Issue 6) should also contain a "Personal Information" section with:
    - Display name (shown in morning brief greeting and other personalised areas)
    - Email address (change account email — triggers Supabase re-verification)
    - Password (change password flow via Supabase)
    - Profile photo / avatar (upload or select an image)
    - Sign out button (moved here from wherever it currently lives)
- All fields editable inline or via a sub-sheet.

ISSUE 9: Research competitor apps and propose UI/UX improvements
Status: Pending
Clarified:
- Research focus: apps that mix second-brain and productivity — Notion, Obsidian, Roam Research, Things 3, Todoist, TickTick, Fantastical, Cron.
- Goal: identify the most successful UI/UX patterns from these apps that MindVault is missing or doing worse.
- Output: present a written list of proposed improvements for user approval BEFORE implementing anything.
- Do not build anything under this issue until the proposal has been reviewed and signed off.
